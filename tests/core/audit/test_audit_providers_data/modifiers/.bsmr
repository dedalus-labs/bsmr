[project]
root = .

[buildfile]
name=TARGETS.fixture

[repositories]
root = .
prelude = prelude

[cell_aliases]
fbsource = root
bsmr = root
fbcode = root
config = prelude
ovr_config = prelude
toolchains = prelude

[external_cells]
prelude = bundled
