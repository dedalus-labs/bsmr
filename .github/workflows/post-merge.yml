# @generated by Hollywood. Do not edit by hand.

name: "PR: Post-merge deployments"
on:
  push:
    branches:
      - main
  workflow_dispatch: {}
permissions:
  actions: read
  contents: read
  issues: write
  pull-requests: read
jobs:
  comment:
    name: Comment deployment runs
    if: github.repository == 'dedalus-labs/bsmr'
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Comment post-merge deployments
        uses: ./.github/actions/ci/post-merge-receipts
        with:
          event-path: ${{ github.event_path }}
          repository: ${{ github.repository }}
          token: ${{ github.token }}
