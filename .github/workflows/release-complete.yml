# @generated by Hollywood. Do not edit by hand.

name: Complete release metadata
on:
  workflow_call:
    inputs:
      plan:
        type: string
        required: true
permissions: {}
jobs:
  complete:
    if: ${{ github.repository == 'dedalus-labs/bsmr' && github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && github.event.inputs.tag != 'dry-run' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - env:
          GH_TOKEN: ${{ github.token }}
        name: Complete release PR
        uses: ./.github/actions/ci/release-complete
        with:
          workspace: ${{ github.workspace }}
          repository: ${{ github.repository }}
          source-sha: ${{ github.sha }}
          event-name: ${{ github.event_name }}
          ref: ${{ github.ref }}
          plan: ${{ inputs.plan }}
