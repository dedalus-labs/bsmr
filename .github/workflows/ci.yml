# @generated by Hollywood. Do not edit by hand.

name: CI
on:
  push:
    branches:
      - main
  pull_request: {}
  merge_group:
    types:
      - checks_requested
  workflow_dispatch: {}
permissions: {}
concurrency:
  group: ${{ format('{0}-{1}', github.workflow, github.ref) }}
  cancel-in-progress: true
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
jobs:
  affected:
    name: Affected paths
    if: ${{ github.repository == 'dedalus-labs/bsmr' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      contents: read
    outputs:
      rust: ${{ steps.check.outputs.rust }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          fetch-depth: 0
      - id: check
        name: Detect Rust changes
        uses: ./.github/actions/ci/rust-affected
        with:
          event-name: ${{ github.event_name }}
          base-sha: ${{ github.event.pull_request.base.sha }}
          head-sha: ${{ github.event.pull_request.head.sha }}
  dependencies:
    name: Dependency review
    if: ${{ github.repository == 'dedalus-labs/bsmr' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && github.event_name == 'pull_request' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Review dependencies
        uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294
        with:
          fail-on-severity: high
  workflows:
    name: Generated workflows
    if: ${{ github.repository == 'dedalus-labs/bsmr' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38
        with:
          node-version: 26.5.1
      - name: Set up pnpm
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093
      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts
      - name: Audit dependencies
        run: pnpm audit --audit-level high
      - name: Check workflow source
        run: pnpm run ci check
  rust_audit:
    name: Rust / Dependencies
    needs: affected
    if: ${{ github.repository == 'dedalus-labs/bsmr' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && needs.affected.outputs.rust == 'true' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Install pinned OSV Scanner
        run: |-
          curl --proto '=https' --tlsv1.2 --fail --location --silent --show-error https://github.com/google/osv-scanner/releases/download/v2.4.0/osv-scanner_linux_amd64 --output "$RUNNER_TEMP/osv-scanner"
          echo "15314940c10d26af9c6649f150b8a47c1262e8fc7e17b1d1029b0e479e8ed8a0  $RUNNER_TEMP/osv-scanner" | sha256sum --check
          chmod 500 "$RUNNER_TEMP/osv-scanner"
      - name: Audit Rust dependencies
        run: |-
          "$RUNNER_TEMP/osv-scanner" scan source --lockfile Cargo.lock --lockfile tools/build/third-party/rust/Cargo.lock --no-resolve --format json --output-file "$RUNNER_TEMP/osv.json" . || [ "$?" -eq 1 ]
          jq -e '[.results[].packages[].vulnerabilities[] | select(any(.affected[]; .database_specific.informational? != "unmaintained"))] as $v | if ($v | length) == 0 then true else ($v | map({id, summary})), false end' "$RUNNER_TEMP/osv.json"
  rust_quality:
    name: Rust / Quality
    needs: affected
    if: ${{ github.repository == 'dedalus-labs/bsmr' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && needs.affected.outputs.rust == 'true' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    permissions:
      contents: read
    env:
      CARGO_INCREMENTAL: "0"
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Install pinned Rust toolchain
        run: rustup toolchain install nightly-2026-04-11 --profile minimal --component clippy --component llvm-tools-preview --component rust-src --no-self-update
      - name: Restore Rust cache
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
        with:
          prefix-key: bsmr-v1
          save-if: false
          shared-key: rust
      - name: Check Rust quality
        run: |-
          python3 test.py --ci --git --lint-rust-only
          python3 test.py --ci --git --rustdoc-only
  rust_tests:
    name: Rust / Tests
    needs: affected
    if: ${{ github.repository == 'dedalus-labs/bsmr' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && needs.affected.outputs.rust == 'true' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    permissions:
      contents: read
    env:
      CARGO_INCREMENTAL: "0"
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Install pinned Rust toolchain
        run: rustup toolchain install nightly-2026-04-11 --profile minimal --component clippy --component llvm-tools-preview --component rust-src --no-self-update
      - name: Restore Rust cache
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
        with:
          prefix-key: bsmr-v1
          save-if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
          shared-key: rust
      - name: Run Rust tests
        run: python3 test.py --ci --git --test-only
  rust_self_host:
    name: Rust / Self-host
    needs: affected
    if: ${{ github.repository == 'dedalus-labs/bsmr' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && needs.affected.outputs.rust == 'true' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    permissions:
      contents: read
    env:
      CARGO_INCREMENTAL: "0"
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - name: Install pinned Rust toolchain
        run: rustup toolchain install nightly-2026-04-11 --profile minimal --component clippy --component llvm-tools-preview --component rust-src --no-self-update
      - name: Restore Rust cache
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
        with:
          prefix-key: bsmr-v1
          save-if: false
          shared-key: rust
      - name: Build BSMR
        run: cargo build --locked --bin bsmr
      - name: Install pinned DotSlash
        run: |-
          curl --proto '=https' --tlsv1.2 --fail --location --silent --show-error https://github.com/facebook/dotslash/releases/download/v0.5.9/dotslash-linux-musl.x86_64.v0.5.9.tar.gz --output "$RUNNER_TEMP/dotslash.tar.gz"
          echo "4c75c6eb7890ae35993b962073f6d9bbe78b42b81a5691303ad70f63bfbf7196  $RUNNER_TEMP/dotslash.tar.gz" | sha256sum --check
          tar -xzf "$RUNNER_TEMP/dotslash.tar.gz" -C "$RUNNER_TEMP"
          echo "$RUNNER_TEMP" >> "$GITHUB_PATH"
      - name: Generate Rust build dependencies
        run: ./tools/bin/reindeer --third-party-dir tools/build/third-party/rust buckify
      - name: Check Starlark
        run: python3 test.py --ci --git --bsmr=target/debug/bsmr --lint-starlark-only
      - name: Validate self-host graph
        run: |-
          target/debug/bsmr --isolation-dir=ci uquery 'deps(//app/...)'
          target/debug/bsmr --isolation-dir=ci targets 'bsmr_build//...'
  rust:
    name: Rust
    if: ${{ always() && github.repository == 'dedalus-labs/bsmr' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs:
      - affected
      - rust_audit
      - rust_quality
      - rust_tests
      - rust_self_host
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions: {}
    steps:
      - name: Accept complete Rust CI
        if: ${{ needs.affected.result == 'success' && (needs.rust_audit.result == 'success' && needs.rust_quality.result == 'success' && needs.rust_tests.result == 'success' && needs.rust_self_host.result == 'success' || needs.rust_audit.result == 'skipped' && needs.rust_quality.result == 'skipped' && needs.rust_tests.result == 'skipped' && needs.rust_self_host.result == 'skipped') }}
        run: "true"
      - name: Reject incomplete Rust CI
        if: ${{ !(needs.affected.result == 'success' && (needs.rust_audit.result == 'success' && needs.rust_quality.result == 'success' && needs.rust_tests.result == 'success' && needs.rust_self_host.result == 'success' || needs.rust_audit.result == 'skipped' && needs.rust_quality.result == 'skipped' && needs.rust_tests.result == 'skipped' && needs.rust_self_host.result == 'skipped')) }}
        run: exit 1
