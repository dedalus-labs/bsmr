# @generated by Hollywood. Do not edit by hand.

name: Publish release
on:
  push:
    branches:
      - main
    paths:
      - .release-please-manifest.json
  workflow_dispatch: {}
permissions: {}
concurrency:
  group: publish-release
  cancel-in-progress: false
jobs:
  publish:
    name: Publish current version
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      actions: write
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: main
          persist-credentials: false
      - id: state
        env:
          GH_TOKEN: ${{ github.token }}
        name: Inspect release state
        uses: ./.github/actions/ci/release-state
        with:
          repository: ${{ github.repository }}
          workspace: ${{ github.workspace }}
      - name: Build and publish release
        if: ${{ steps.state.outputs.state == 'absent' }}
        env:
          GH_TOKEN: ${{ github.token }}
          HOLLYWOOD_COMMAND_ARG_4: ${{ github.repository }}
          HOLLYWOOD_COMMAND_ARG_8: ${{ format('tag={0}', steps.state.outputs.tag) }}
        run: gh workflow run release.yml --repo "$HOLLYWOOD_COMMAND_ARG_4" --ref main --field "$HOLLYWOOD_COMMAND_ARG_8"
        shell: bash
