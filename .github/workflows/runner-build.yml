# @generated by Hollywood. Do not edit by hand.

name: Build Rust on Mac
run-name: ${{ format('Rust build {0}', github.event_name == 'push' && github.sha || inputs.revision) }}
concurrency:
  group: ${{ github.event_name == 'push' && 'rust-mac-main' || format('rust-mac-{0}', github.run_id) }}
  cancel-in-progress: true
on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      revision:
        description: Exact source SHA approved by the administrator.
        type: string
        required: true
      provider:
        description: Capacity provider, or automatic selection.
        type: choice
        options:
          - auto
          - machines
          - blacksmith
          - github
        default: auto
      definition:
        description: Parent workflow definition SHA for owned child dispatches.
        type: string
        default: ""
      parent:
        description: Parent workflow run ID for owned child dispatches.
        type: string
        default: ""
permissions:
  contents: read
jobs:
  authorize:
    name: Verify build authorization
    if: ${{ github.repository == 'dedalus-labs/bsmr' && github.ref == 'refs/heads/main' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - env:
          GH_TOKEN: ${{ github.token }}
        name: Verify build ownership
        uses: ./.github/actions/runner/build
        with:
          operation: authorize
          provider: ${{ github.event_name == 'push' && 'auto' || inputs.provider }}
          source: ${{ github.event_name == 'push' && github.sha || inputs.revision }}
          definition: ${{ inputs.definition }}
          actual-definition: ${{ github.sha }}
          repository: ${{ github.repository }}
          event: ${{ github.event_name }}
          ref: ${{ github.ref }}
          actor: ${{ github.triggering_actor }}
          parent: ${{ inputs.parent }}
  dispatch:
    name: Select Mac build capacity
    needs: authorize
    if: ${{ (github.event_name == 'push' && 'auto' || inputs.provider) == 'auto' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 75
    permissions:
      contents: read
      actions: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - id: machines_dispatch
        if: ${{ success() }}
        env:
          GH_TOKEN: ${{ github.token }}
        name: Request Dedalus Machines runner
        uses: ./.github/actions/runner/build
        with:
          operation: dispatch
          provider: machines
          source: ${{ github.event_name == 'push' && github.sha || inputs.revision }}
          definition: ${{ github.sha }}
          parent: ${{ github.run_id }}
      - id: machines_wait
        if: ${{ success() }}
        env:
          GH_TOKEN: ${{ github.token }}
        name: Wait for Dedalus Machines build
        uses: ./.github/actions/runner/build
        with:
          operation: wait
          provider: machines
          run: ${{ steps.machines_dispatch.outputs.id }}
      - if: ${{ always() }}
        env:
          GH_TOKEN: ${{ github.token }}
        name: Clean up Dedalus Machines attempt
        uses: ./.github/actions/runner/build
        with:
          operation: cleanup
          run: ${{ steps.machines_dispatch.outputs.id }}
      - id: blacksmith_dispatch
        if: ${{ steps.machines_wait.outputs.result == 'unassigned' }}
        env:
          GH_TOKEN: ${{ github.token }}
        name: Request Blacksmith runner
        uses: ./.github/actions/runner/build
        with:
          operation: dispatch
          provider: blacksmith
          source: ${{ github.event_name == 'push' && github.sha || inputs.revision }}
          definition: ${{ github.sha }}
          parent: ${{ github.run_id }}
      - id: blacksmith_wait
        if: ${{ steps.machines_wait.outputs.result == 'unassigned' }}
        env:
          GH_TOKEN: ${{ github.token }}
        name: Wait for Blacksmith build
        uses: ./.github/actions/runner/build
        with:
          operation: wait
          provider: blacksmith
          run: ${{ steps.blacksmith_dispatch.outputs.id }}
      - if: ${{ always() }}
        env:
          GH_TOKEN: ${{ github.token }}
        name: Clean up Blacksmith attempt
        uses: ./.github/actions/runner/build
        with:
          operation: cleanup
          run: ${{ steps.blacksmith_dispatch.outputs.id }}
      - id: github_dispatch
        if: ${{ steps.blacksmith_wait.outputs.result == 'unassigned' }}
        env:
          GH_TOKEN: ${{ github.token }}
        name: Request GitHub runner
        uses: ./.github/actions/runner/build
        with:
          operation: dispatch
          provider: github
          source: ${{ github.event_name == 'push' && github.sha || inputs.revision }}
          definition: ${{ github.sha }}
          parent: ${{ github.run_id }}
      - id: github_wait
        if: ${{ steps.blacksmith_wait.outputs.result == 'unassigned' }}
        env:
          GH_TOKEN: ${{ github.token }}
        name: Wait for GitHub build
        uses: ./.github/actions/runner/build
        with:
          operation: wait
          provider: github
          run: ${{ steps.github_dispatch.outputs.id }}
      - if: ${{ always() }}
        env:
          GH_TOKEN: ${{ github.token }}
        name: Clean up GitHub attempt
        uses: ./.github/actions/runner/build
        with:
          operation: cleanup
          run: ${{ steps.github_dispatch.outputs.id }}
      - env:
          GH_TOKEN: ${{ github.token }}
        name: Require a successful build
        uses: ./.github/actions/runner/build
        with:
          operation: complete
          result: ${{ steps.github_wait.outputs.result || steps.blacksmith_wait.outputs.result || steps.machines_wait.outputs.result }}
  build:
    name: Build Rust
    needs: authorize
    if: ${{ github.repository == 'dedalus-labs/bsmr' && github.ref == 'refs/heads/main' && (github.event_name == 'push' && 'auto' || inputs.provider) != 'auto' }}
    runs-on: ${{ fromJSON('{"machines":{"group":"Dedalus Machines","labels":["self-hosted","macOS","ARM64","dedalus-machines"]},"blacksmith":"blacksmith-12vcpu-macos-15","github":"macos-15"}')[inputs.provider] }}
    timeout-minutes: 60
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - env:
          GH_TOKEN: ${{ github.token }}
        name: Verify build ownership
        uses: ./.github/actions/runner/build
        with:
          operation: authorize
          provider: ${{ github.event_name == 'push' && 'auto' || inputs.provider }}
          source: ${{ github.event_name == 'push' && github.sha || inputs.revision }}
          definition: ${{ inputs.definition }}
          actual-definition: ${{ github.sha }}
          repository: ${{ github.repository }}
          event: ${{ github.event_name }}
          ref: ${{ github.ref }}
          actor: ${{ github.triggering_actor }}
          parent: ${{ inputs.parent }}
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38
        with:
          node-version: 26.5.1
      - name: Verify native Mac architecture
        run: node -e 'const a = require('"'"'node:assert/strict'"'"'); a.equal(process.platform, '"'"'darwin'"'"'); a.equal(process.arch, '"'"'arm64'"'"');'
        shell: bash
      - name: Install Rust tooling
        uses: ./.github/actions/rust/install
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.event_name == 'push' && github.sha || inputs.revision }}
          persist-credentials: false
      - name: Install pinned Rust compiler
        run: rustup toolchain install nightly-2026-04-11 --profile minimal --no-self-update
        shell: bash
      - name: Restore engine cache
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
        env:
          CARGO_PROFILE_DEV_DEBUG: "0"
          CARGO_INCREMENTAL: "0"
        with:
          prefix-key: bsmr-v2
          shared-key: rust
          cache-bin: false
          save-if: ${{ (github.event_name == 'push' && github.sha || inputs.revision) == github.sha }}
      - name: Build BSMR
        env:
          CARGO_PROFILE_DEV_DEBUG: "0"
          CARGO_INCREMENTAL: "0"
        run: cargo build --locked --bin bsmr -j 2
        shell: bash
      - name: Install planner compiler
        run: rustup toolchain install 1.98.0 --profile minimal --no-self-update
        shell: bash
      - name: Restore planner cache
        uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
        env:
          CARGO_PROFILE_DEV_DEBUG: "0"
          CARGO_INCREMENTAL: "0"
          RUSTUP_TOOLCHAIN: 1.98.0
        with:
          prefix-key: bsmr-v1
          shared-key: planner
          cache-bin: false
          workspaces: tools/cargo -> target
          save-if: ${{ (github.event_name == 'push' && github.sha || inputs.revision) == github.sha }}
      - name: Build Cargo planner
        env:
          CARGO_PROFILE_DEV_DEBUG: "0"
          CARGO_INCREMENTAL: "0"
        run: rustup run 1.98.0 cargo build --locked --manifest-path tools/cargo/Cargo.toml --target-dir tools/cargo/target -j 2
        shell: bash
      - name: Install Cargo planner
        run: cp tools/cargo/target/debug/bsmr-cargo target/debug/bsmr-cargo
        shell: bash
      - name: Install qualification compiler
        run: rustup toolchain install 1.97.1 --profile minimal --no-self-update
        shell: bash
      - name: Verify compiler archive metadata
        run: python3 test/rust/catalog.py target/debug/bsmr
        shell: bash
      - name: Verify native Rust builds
        run: node test/native-rust-build.ts target/debug/bsmr
        shell: bash
      - name: Verify configured Cargo builds
        run: node test/rust/configured.ts target/debug/bsmr
        shell: bash
      - name: Verify Rust library formats
        run: node test/rust/libraries.ts target/debug/bsmr
        shell: bash
