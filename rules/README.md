# Rules

This directory contains first-party build rules that Bessemer owns, tests, and
versions independently from the build engine.

`android/` is reserved for Android rules backed by a public SDK toolchain.
Android support belongs here once a clean checkout can build and test the
example application without Meta-only targets or infrastructure.
